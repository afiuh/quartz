---
---



> 状态：想法阶段 | 来源：bili-AI-tool零耦合架构的延伸思�? 
> 核心关系：陈懿灵（设计者）�?AI Hermes（讨论�?记录者）

---

## 一、想法的核心

设计一�?*能在代码层面自我进化**的智能体�?
智能体的所有功能都拆成零耦合模块。模块之间有统一的数据协议。智能体本身可以为自己生成新模块，用户也可以为智能体添加新模块。能力不是预定义的——是生长出来的�?
---

## 二、为什么现有智能体做不�?
当前所有AI智能体（ChatGPT插件、Claude tool use、各种Agent框架）都�?*能力边界固定的工具调用器**�?
功能模块必须由人类预先写好、预先定义接口、预先部署。智能体只能调用给定的工具列表，不能给自己添加新能力�?
用户说“我需要处理Excel”——智能体做不了。只能等人类去写模块，然后注册进来�?
**智能体的能力是凝固的，不是生长的�?*

---

## 三、零耦合架构如何解决这个问题

零耦合 = 模块之间不import，只通过标准数据协议通信�?
这意味着�?
- 添加新模�?*不需要修改任何现有模块的代码**
    
- 智能体生成的模块是一�?*孤立的文�?*，不会污染现有系�?    
- 只要输入输出符合协议，就能接�?    

就像bili-AI-tool里加新功能模块——新建一�?py，读写缓存文件夹，不需要动scoring.py或transcribe_worker.py。智能体也可以做同样的事�?
---

## 四、框架设计要�?
### 4.1 内核极薄

框架本身不提供任何业务功能，只维护三样东西：

- **模块注册�?*：哪些模块存在、能处理什么类型的数据
    
- **数据协议**：模块之间通信的标准（输入/输出JSON schema�?    
- **调度�?*：根据任务类型匹配对应的模块
    

没有依赖注入、没有中间件、没有工厂模式。零耦合本身就是所有设计模式的终点�?
### 4.2 模块平等，包括智能体自身

每个模块结构相同�?
- 输入接口（读缓存或消息）
    
- 输出接口（写缓存或消息）
    
- 能力声明（告诉调度器“我能处理什么类型的数据”）
    

没有“核心模块”和“外围模块”之分�?*智能体核心本身也是一个模�?*——一个声明自己能做“决策和调度”的模块。它与Excel处理模块、PDF解析模块在框架眼里没有区别�?
### 4.3 能力是生长出来的

框架启动时可能只有三个模块：

- 智能体核心（决策+调度�?    
- 文件读写（缓存层�?    
- 代码执行（沙箱）
    

用户说“帮我处理Excel�?�?智能体发现自己没有这个能�?�?按数据协议写一个excel_handler.py �?放到模块目录�?�?调度器自动发现并注册 �?从此智能体能处理Excel�?
就像生物从受精卵开始，不断分裂出新细胞——每个细胞携带相同的DNA（数据协议），但分化出不同功能�?
### 4.4 协议即DNA

数据协议是整个系统的遗传物质，极简但足以描述一切：

- 每个模块的输入是标准化的JSON
    
- 输出也是标准化的JSON
    
- 错误用统一格式返回
    
- 模块声明自己能处理的“数据类型”和“处理级别�?    

协议一旦确立，就是系统的宪法。所有模块——无论是人类写的还是智能体自己生成的——必须遵守�?
---

## 五、从工具到生态的跃迁

如果这个框架开源，可以发展成一�?*智能体共创生�?*�?
### 5.1 协作门槛降到最�?
不需要理解全局代码，只需要理解协议就能贡献。懂Excel的人写excel_handler，懂音频的人写audio_transcriber，懂某个小众领域的人写domain_expert—�?*每个人只做一块积木，所有积木拼成一个智能体�?*

### 5.2 智能体自己寻找新能力

执行任务时发现缺某种能力 �?智能体去模块市场搜索 �?推荐给用户：“主人，有一个模块能解决这个问题，评�?.8，要我安装吗？�?�?你确�?�?自动拉取、注册、执行�?
**智能体不只是被养——它会自己找食物�?*

### 5.3 与现有开源生态的本质区别

现有开源生态：人找项目 �?人读文档 �?人写代码 �?人提PR�?
这个生态：**人写模块 �?智能体发现模�?�?智能体推荐模�?�?智能体调用模块�?*

人的角色从“执行者”变成“能力贡献者”。你贡献一个PDF模块，不仅你自己的智能体能用——所有人的智能体都能用。每一次贡献都在让整个智能体种群变得更强�?
**从“开源软件”到“开源能力”�?*

---

## 六、可行性判�?
### 6.1 原型可以一个人完成

代码量预估：

- 内核（注册表+协议+调度器）：几百行Python
    
- 初始三个模块：几百行
    
- 总代码量：原型阶段约1000-2000�?    

主要精力不在写代码，�?*设计数据协议**——这是整个系统的DNA，一旦定下，所有未来模块按此生长�?
### 6.2 已验证的架构基础

bili-AI-tool已经验证了：

- 零耦合模块协作�?个模块通过缓存文件通信�?    
- 双层数据协议（_search批量文件 + candidates递进文件�?    
- 智能体操作独立模块的模式
    

现在是把这个项目级的验证，升级为通用框架�?
### 6.3 需要解决的工程问题

- **协议版本演化**：当模块增多，协议需要升级时，如何保证向后兼�?    
- **模块健康度管�?*：如何评估模块质量、使用频率，淘汰无用的或冲突的模�?    
- **安全边界**：智能体生成的代码在沙箱中执行，模块只能通过协议通信，天然限制风�?    

---

## 七、想法由�?
从UNIX设计
