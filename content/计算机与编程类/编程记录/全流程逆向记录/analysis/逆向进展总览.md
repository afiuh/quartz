---
---

# ZoomPlus 逆向进展总览

> 更新日期�?026-08-05（深夜会话）
> 早期静态分析详见《ZoomPlus逆向分析报告.md》；本文件是**当前进度权威文档**�?
## 1. 项目状态一句话

已完成程序身份确认、模块地图重建、瞄准核心管线（目标选择→瞄准点→PID）的**行为等价逆向与验�?*�?贝塞尔公式、输出通道、外围模块（ai_loop/auto_fire/GUI 等）待续�?
## 2. 已完成并验证的成�?
| 功能 | 验证方式 | 结果 |
|---|---|---|
| 程序性质（Nuitka standalone，非 PyInstaller�?| PE/导入�?特征�?| �?确认 |
| 载荷结构（zlib→ZIP→目录） | 解包 + 哈希比对 | �?439 文件 1:1 |
| 模块地图（core/gui/win_utils 全清单） | 字符串表 | �?50 个应用模�?|
| 反作弊检测名单（14 项） | 字符�?| �?|
| Arduino 伪装罗技 G502 | 字符�?| �?|
| `is_head_class` | Frida 实采 | �?关键词全小写、大小写不敏�?|
| `calculate_aim_target` | Frida 实采 | �?1086+ 样本零误�?|
| `process_aiming` 状态级行为 | Frida 前后 LoopState | �?374 组回放通过 |
| `PIDController.update` 完整公式 | Frida 2032 组回�?| �?2032/2032 零误�?|

## 3. 已确认的核心公式

### 3.1 calculate_aim_target（已验证�?```python
x1, y1, x2, y2 = box
center_x = x1 + 0.5*(x2-x1)
if class_name is not None and is_head_class(class_name):
    return center_x, y1 + 0.5*(y2-y1)       # 头部�?�?中心
return center_x, y1 + aim_y_offset_ratio*(y2-y1)  # 其他 �?高度偏移
```

### 3.2 PIDController.update（已验证�?032/2032�?```python
e = 0 if |error| < 0.25 else error                    # 死区
if 符号翻转(error×prev < 0): integral = clamp(e, ±80)  # 方向反转重置
else: integral = clamp(integral + e, ±80)             # 积分限幅
fd = 0.35×(e �?prev) + 0.65×fd                        # 微分低�?α=0.35
output = Kp×e + Ki×integral + Kd×clamp(fd, ±60)       # 微分限幅 60
prev = e
```
属性名 `Kp/Ki/Kd`（大写）；常量池 .rdata 已定位（0.25/80/60/0.35 相邻）�?
### 3.3 process_aiming 管线（状态级已验证）
```
for idx, box in enumerate(boxes): 类别过滤(class_ids[idx]∈enabled)
  �?calculate_aim_target �?error �?distance_sq �?候�?距离,目标x,目标y,box)
valid_targets.sort(key=距离) �?取最�?无目�?�?target_locked=False 返回
有目�?�?锁定转变�?target_locked=True + bezier_curve_scalar=uniform(-1,1)
  �?tracker_last_time=now, tracker_last_target_box=box
  �?pid_x.update(ex), pid_y.update(ey)
  �?bezier �?int(round()) �?invert �?全零跳过 �?xbox/send_mouse_move
```

### 3.4 重要修正�?026-08-05）：5 参数调用不是贝塞尔，是磁�?
此前误以�?`process_aiming` 里那�?5 参数调用（error_x, error_y, box, strength, steps）是贝塞尔，
目标函数候选为 `FUN_140758840`�?*已证�?*�?
- `core.ai_aiming` 模块初始�?`FUN_140760900` 只注�?4 个函数：
  `calculate_aim_target`(FUN_140758ea0) / `is_head_class`(FUN_14075a250) /
  `calculate_sticky_pull`(FUN_14075a690) / `process_aiming`(FUN_14075b770)�?- getter `FUN_140758840` 的字符串键（DAT_1433435d8）与注册表中�?  `calculate_sticky_pull` 完全一�?�?�?5 参数调用就是**磁吸**，不是贝塞尔�?- 贝塞尔实现不�?`core.ai_aiming` 里，需�?`ai_loop`/`ai_loop_utils` 或输出通道继续找�?
注：�?getter 引用�?`DAT_143343xxx` 在文件中是运行时填充的全局槽（静态字节全零）�?Ghidra 无法通过引用还原字符串键；配置键 `bezier_curve_enabled/strength/steps` �?读取者也因同样原因静态定位失败（12 �?"Bezier" 字符串均无引用）�?
### 3.5 动态采集首轮结果（2026-08-05 深夜，capture_bezier v4�?
**输出通道确认**：程序实际加�?`dd63330.dll`（与 `ddxoft.dll` 哈希完全相同），
运行时导出为 DD 驱动 API：`DD_btn / DD_key / DD_mov / DD_movR / DD_str / DD_todc / DD_whl`�?自瞄相对移动�?**`DD_movR(x, y)`**（驱动内部再�?DeviceIoControl，SendInput/mouse_event 不触发）�?
**采集数据**�?7 �?/ 200 �?PID 更新 / 50 �?process_aiming 采样 / 76 �?DD_movR 输出帧�?参数：strength=0.16、steps=4（全程不变）、scalar 14 个取�?�?[-0.75, 0.95]、bezier 全程开启�?
**核心结论**�?6/76 已发送帧的输�?*全部等于 int(round(PID 输出))**（含反转逻辑），
贝塞尔对取整后的结果**零可见影�?*。但本组 PID 输出很小（|x|�?.75、|y|�?.74），
贝塞尔的亚像素改动可能被取整掩盖——无法判定公式为恒等或未生效，需要更大输出样本验证�?
分析脚本/产物�?- `analysis/frida/capture_bezier.py`（v4 采集）、`analyze_bezier_capture.py`（seq 顺序配对�?- `analysis/frida/bezier_capture.txt`（原始）、`bezier_capture_aligned_v2.csv`�?6 帧配对表�?
**教训**：重启采集时强杀 Frida 进程会导致目标进程卡死（注入残留），必须优雅 detach
或在目标重启后再挂载，禁止在采集运行中强杀�?
### 3.6 动态采集第二轮结果�?026-08-06 凌晨，决定性结论）

**数据规模**�?0,047 事件 / 5,920 �?PID 更新 / 2,635 �?DD_movR 输出帧（全部正确配对）�?参数覆盖：strength �?{0.13, 0.16, 0.92}，steps �?{3, 4, 16}，贝塞尔开 1643 �?/ �?992 帧，
scalar 71 个取�?�?[-0.96, 0.99]，PID 输出最�?|128|（大移动覆盖充分）�?
**核心结论（强�?*�?635/2635 已发送帧的输�?*全部等于 int(round(PID 输出))**，零偏差�?即使强度 0.92 / 步数 16 / PID 输出 33.6�?50 帧）以及 PID 输出 128�?68 帧）也无一例外�?�?**贝塞尔在 ddxoft 鼠标路径上不生效（或公式恒等�?*，且�?strength/steps/scalar 取值无关�?
**win_utils 模块函数清单**（模块限定名）：admin / arduino_mouse / arduino_spoofer / console /
ddxoft_mouse / gamepad_input / key_utils / makcu_mouse / mouse_click / mouse_move /
priority / vk_codes / xbox_controller�?*无任�?bezier 函数**，输出层只有
`mouse_move`（按方法分发 ddxoft/arduino/makcu/win32）与 `xbox_controller`�?
**剩余假设**（按可能性排序）�?1. 贝塞尔只作用于手柄（xbox_controller）输出路径；
2. 贝塞尔作用于瞄准�?预测路径（影�?PID 的输入误差，输出层不可见）；
3. 配置键被读取但功能已死代�?未启用�?
下一步候选：a) �?配置键读取追�?钩子（谁在读 bezier_curve_* 一目了然）�?b) �?xbox 输出路径实测；c) 先搁置贝塞尔，继续还�?win_utils.mouse_move 输出通道�?
### 3.7 用户反馈 + 抖动机制确认�?026-08-06�?
用户确认�?*贝塞尔开启后瞄准会左右晃动（类人手抖动）**——功能真实生效�?
抖动分析（analyze_sway.py）在 on/0.13/3 段发现强振荡：dx 自相�?-0.38（负=高频左右抖）�?�?PID 输出与发送值自相关一致（-0.38）→ **振荡源头�?PID 输入误差（瞄准点被贝塞尔扰动），
PID 只是忠实跟踪**。这�?输出==round(PID)"的观测完全自洽：贝塞尔作用在误差/瞄准点侧�?不在输出侧。步�?3（粗）振荡、步�?16（细）平滑，符合曲线插值行为�?
旁证：帮助文档含 "response curves"（Xbox 输出支持响应曲线）→ 贝塞尔可能同�?主要
作用于手柄路径，但鼠标路径的抖动说明它也在影响误差侧，待探测确认�?
新增工具：`analysis/frida/probe_bezier_reads.py`——hook PyObject_GetAttr/GetAttrString/
GetItem/DictGetItemString，只匹配 bezier_curve_* 键并抓调用栈（模块内偏移），
一次性定位贝塞尔宿主函数。产�?`bezier_reads.txt`�?
### 3.8 静态还原进�?+ 交叉验证结论�?026-08-06�?
**静态已完成**�?- 贝塞�?process_aiming 内联公式，读取点 0x75e065(enabled)/0x75e294(strength)�?  运算结构（反汇编确认）：`neg(R12)` �?`mul(neg,float(strength))` �?  `mul(·, getattr(obj, KEY))` ×2 �?两次 `iadd`。即
  `result1 = R15 - R12·strength·Z`，`result2 = R12 + R15·strength·Z2`
  （Z/Z2 = 两处 getattr 值，KEY �?0x1433435a8，推测为 state.bezier_curve_scalar�?  scalar 不经 PyObject_GetAttr 读取=�?getattro 快速路径，探测未捕获到，吻合）�?- 助手语义确认�?x23daf70/0x23dae40=乘法�?x23d8020=加法�?x23dec20=浮点就地加法�?  0x12a30=一元运算跳板�?x1423bfb30=float()�?x1423d4c10=getattr�?x1423dc8a0=减法�?- 误差通路：calculate_aim_target �?**SmartTracker 调整** �?贝塞�?�?PID�?  对照组（bezier 关）X 通道 152 帧精确等�?box目标-960，Y 通道任何候选公式都
  拟合不上（残�?std�?px）→ 追踪器对 Y 有逐帧调整�?- SmartTracker 模块初始�?FUN_1407fd490；update= FUN_1407fae10�?71 行，
  含速度/预测运算序列）；其余方法已反编译待识别�?- _calculate_adjusted_kp 确认�?PIDController 方法（字符串
  `PIDController._calculate_adjusted_kp`），inference 注册�?12 个方法已反编译�?- 综合采集脚本 `capture_all.py` 就绪�? 个贝塞尔调用点寄存器级捕�?+
  注册表运行时解析（SmartTracker/动态P/ai_loop 自动挂钩�? 输出通道 + PID�?
**交叉验证（现有数据，919 �?bezier_开 帧）**�?- 测试 ex' = ex �?S·ey（S=strength·scalar，及 strength·ey/ey/scalar·ey 变体�?  回归 d=mod_ex-true_ex：全�?R²<0.01，斜率不显著 �?**旋转假设被否�?不可�?*�?- 结论：贝塞尔具体操作数（Z/Z2 的值、R12/R15 的身份、XY 通道映射）无法从旧数�?  判定（追踪器逐帧调整污染误差通道）→ **必须依赖 capture_all 的一次算术采�?*�?- 动态范围收敛：仅需 1 次会话（静态目标贝塞尔开 1 分钟 + 动态目�?1 分钟 +
  贝塞尔关 30 �?[+ xbox 可选]），即可覆盖贝塞�?追踪�?动态P/输出全部缺口�?
### 3.9 贝塞尔公式确�?+ 采集崩溃事件�?026-08-06�?
**贝塞尔公式（已确认，寄存器级采集�?*�?```
S = config.bezier_curve_strength * state.bezier_curve_scalar
ex' = ex - S*ey
ey' = ey + S*ex
```
即误差向量旋�?angle �?S，与"左右�?行为吻合。证据链�?- 静态骨架：result1 = R15 - R12*strength*Z，result2 = R12 + R15*strength*Z2�?- 运行时键�?0x1433435a8 = "bezier_curve_scalar"（诊断确认）�?- 崩溃前采集的操作数：rsi=strength（恒定）、rdi=scalar、r12/r15=误差�?  neg/mul 运算链完全对应骨架；r15≈PID误差x（旋转量小时近似成立）→ R15=ex、R12=ey�?- 已写�?reconstructed/core/ai_aiming.py `_bezier_move`，测�?4 项通过（�?10/10）�?
**采集崩溃事件（教训）**�?- 12:28 frida-agent.dll 崩溃 = 诊断脚本附加挂起所致（操作失误）；
- 12:45 / 12:50 python312.dll+0x55b1c0 崩溃（NULL+8 访问）＝**贝塞尔调用点钩子
  （Frida 在指令中段拦�?CALL）破坏目标函数环�?*。前两轮无此钩子的采�?  （capture_bezier v4）跑 14 分钟稳定，佐证�?- 处理：capture_all 已移除贝塞尔调用点钩子（BEZIER_SITES=[]），公式已解出无需再采�?  钩子回调已全面加固（对象合法性预检 isPlausibleObject + try/catch）�?
**剩余动态项**：SmartTracker 精确公式（语义版已写，函数入口钩子属安全类型�?  待用户时间允许时单独采集）；_calculate_adjusted_kp（低优先）；xbox（可选）�?
### 3.10 SmartTracker 结构验证 + process_aiming 集成确认�?026-08-06�?691 帧）

**SmartTracker（capture_all 安全版实采验证）**�?- `update(target_x, target_y, dt) -> (x, y, vx, vy)`（dt=帧间隔）�?- `get_predicted_position(prediction_time) -> (x+vx*pt, y+vy*pt)` **4691/4691 帧吻�?*�?- 速度平滑 v = alpha*raw + (1-alpha)*prev，alpha 随帧间隔变化
  （实测中位数：dt�?.01�?.32、dt�?.02�?.71、长间隔�?.0 复位）；
- is_in_deadzone / get_corrected_move 存在但瞄准路径未调用�? 次）�?
**process_aiming 集成（修正）**�?- 误差 = 追踪器预测位�?�?准星（pred �?960+error_x, 540+error_y 配对吻合）；
- **贝塞尔在 PID 之前**（误差侧）——修正了此前"PID 之后"的错误顺序，
  �?2635 帧输�?=round(PID 返回)"证据一致；
- 重建代码已更新（smart_tracker.py / ai_aiming.py），测试 10/10 通过�?
**_calculate_adjusted_kp（部分确认）**：每帧每�?PID 都调�?(kp, error)�?误差 �?5.6 时返回原值；大误差段�?0-50% 线�?/ 50-100% 加速至 200%"公式
仍缺样本，保持待验证�?
## 4. 测试套件�?/6 通过�?
`reconstructed/tests/`：`python -m unittest discover -s reconstructed/tests`

- test_golden_samples（calculate_aim_target 黄金样本�?- test_head_class_uses_center
- test_offset_ratio_positioning
- test_known_labels（is_head_class�?- test_replay_state_transitions（process_aiming 374 组）
- test_replay（PIDController 2032 组）

## 5. 待办事项

| 优先�?| 事项 | 状�?|
|---|---|---|
| �?| 贝塞尔函数内部公式（机制已确�?扰动 PID 输入误差；探测脚本就绪，待靶场定位宿主函数） | 待探�?|
| �?| `win_utils.send_mouse_move` / `set_ai_overlay_target_xbox` | 未做 |
| �?| `SmartTracker.update` 内部算法（状态层已验证） | 未还�?|
| �?| `_calculate_adjusted_kp` 动�?P（代码存在，样本未触发） | 标记待确�?|
| �?| 磁吸 `calculate_sticky_pull` | 用户决定搁置 |
| �?| 外围模块（ai_loop/auto_fire/config/updater/GUI�?| 未做 |

## 6. 工具链与工作�?
- Ghidra 12.1.2（英文路径联�?`C:\Users\c3458\Ghidra12`，工�?`C:\Users\c3458\GhidraWork\ZoomPlusProj`�?- 脚本：`analysis/ghidra_scripts/nuitka_locate.java`（字符串定位函数）、`nuitka_decompile.java`（反编译导出�?- Frida 16.7.19（`tool/frida_env`）；采集脚本 `analysis/frida/capture_ai_aiming.py`（v3�? 钩子 + 前后状�?+ PID __dict__�?- 采集数据：`analysis/frida/ai_aiming_capture.txt`（数千组样本�?- 运行命令见《命令手�?md�?
## 7. 文件索引

| 内容 | 路径 |
|---|---|
| 重建源码 | `reconstructed/core/ai_aiming.py`、`inference.py`、`smart_tracker.py`、`auto_fire.py` |
| process_aiming 笔记 | `reconstructed/core/process_aiming_note.md` |
| 测试 | `reconstructed/tests/` |
| 早期静态报�?| `analysis/ZoomPlus逆向分析报告.md` |
| 命令手册 | `analysis/命令手册.md` |
| 反编译产�?| `C:\Users\c3458\GhidraWork\decompiled_*` |

