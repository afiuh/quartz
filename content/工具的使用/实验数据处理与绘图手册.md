---
---

**工具�?*：VS Code（含 Rainbow CSV�?+ Origin  
**流程概览**：检查清�?�?筛选整�?�?导入 Origin �?绘图美化 �?导出
[数据记录](file:///D:%5C科研%5C数据记录)
---

### 🔍 第一步：�?VS Code 中检查与清洗数据

拿到仪器导出的 `.csv` 或 `.txt` 文件后，先用 VS Code 做“预检”，能让后续 Origin 绘图零报错�?
| 操作            | 方法                                                                    | 对应快捷�?指令                        |
| ------------- | --------------------------------------------------------------------- | ------------------------------- |
| **打开文件**      | 直接将文件拖�?VS Code 窗口。若是 `.txt` 且为表格，点击右下角语言模式，选 `CSV`，让 Rainbow CSV 着色�?| `Ctrl+K M` �?输入 `CSV`           |
| **快速定位分隔符错乱** | 彩虹色会为每列分配不同颜色，若出现颜色断层，说明逗号错位�?                                        | �?                              |
| **批量替换分隔�?*   | 如果数据用空格或制表符分隔，按 `Ctrl+H`，查找栏输入原分隔符（如一个空格），替换栏输入 `,`，点击全部替换�?          | `Ctrl+H`                        |
| **删除文件头注�?*   | 直接选中仪器信息行（如以 `//` 开头），按 `Delete`�?                                    | �?                              |
| **清洗后保�?*     | 按 `Ctrl+S` 保存。若原为 `.txt`，建议 `Ctrl+Shift+S` 另存为 `.csv`�?               | `Ctrl+Shift+S` �?文件名以 `.csv` 结尾 |

---

### 🌈 第二步：�?Rainbow CSV 高级功能整理数据

清洗完基础格式后，利用 Rainbow CSV 的专属功能，可以像操作数据库一样处理数据�?
#### 1. 分隔符控�?
|目的|指令|快捷�?入口|
|---|---|---|
|临时指定分隔符|`Rainbow CSV: Set delimiter`|`Ctrl+Shift+P` �?输入指令 �?输入 `;` 或 `,`|
|将光标选中字符设为分隔符|`Rainbow CSV: Set rainbow separator`|选中分隔�?→ `Ctrl+Shift+P` �?指令|
|手动输入自定义分隔符|`Rainbow CSV: Choose dynamic separator`|`Ctrl+Shift+P` �?指令 �?输入任意字符|

#### 2. 列对齐与修剪

|指令|作用|
|---|---|
|`Rainbow CSV: Align`|用空格填充，让各列在视觉上对齐|
|`Rainbow CSV: Shrink`|去除每列首尾多余的空格|

#### 3. RBQL 查询语言（类 SQL�?
**启动方式**：`Ctrl+Shift+P` �?输入 `Rainbow CSV: RBQL` 并回车�? 
**列引�?*：`a1` 为第 1 列，`a2` 为第 2 列……表头行不参与计数�?
|常用查询示例|功能|
|---|---|
|`SELECT *`|查看全部数据|
|`SELECT a1, a3 WHERE a4 > 2.4`|筛选第 4 列值大�?2.4 的行，只保留�?1�? 列|
|`SELECT * ORDER BY a3 DESC`|按第 3 列降序排列|
|`UPDATE SET a5 = '已复�? WHERE a4 < 2.3`|将符合条件行的第 5 列改为“已复核”|
|`SELECT a1, a2 * 10 WHERE a1 == "Buy" ORDER BY Number(a2) DESC LIMIT 100`|计算�?2 列�?0，筛选第 1 列为 "Buy" 的行，降序并限制 100 行|

查询结果会出现在新窗口，你可以直接 `Ctrl+S` 保存为新�?CSV，再导入 Origin�?
#### 4. 数据一致性检�?
点击 VS Code 底部状态栏的�?*`CSVLint`** 按钮，可以检查每行列数是否一致，避免导入 Origin 时出错�?
---

### 📥 第三步：将整理好�?CSV 导入 Origin

1. **拖拽导入**  
    �?VS Code 中保存的 `.csv` 文件直接拖入 Origin 灰色工作区，弹出 **Import and Export: impCSV** 对话框�?    
2. **关键设置确认**
    
    - **Header Lines**：若第一行是表头（温度、电压等），保持 `Long Names` 为 `1`；若无表头，改为 `0`�?        
    - **Separator**：确保为 `Comma`�?        
    - 下方预览窗口确认数据列已正确分开�?        
3. **点击 `OK`**  
    数据整齐出现�?Origin �?Book 中。此时你还可以在 Origin 表格里做最终修改（如添加单位、修改列名）�?    

---

### 📈 第四步：�?Origin 中绘图与美化

1. **选中数据�?*  
    在表格中点击列标题选中 Y 轴数据（可按住 `Ctrl` 多选）。Origin 默认最左侧列为 X 轴�?    
2. **一键出�?*  
    点击界面左下方工具栏的图形按钮：**折线�?* / **散点�?* / **点线�?*，图形立即生成�?    
3. **美化原则：双击一�?*
    
    - **线条/符号**：双击线条或数据点，调整颜色、粗细、形状�?        
    - **坐标�?*：双击坐标轴数字，修改范围、字体、轴标题�?        
    - **图例**：点击图例文字直接编辑内容�?        
    - **画布尺寸**：菜单栏 `Format` → `Page` 设置期刊所需的宽高�?        
4. **导出图片**  
    右键图形空白�?→�?*Export Graphs...** �?选择格式（`PNG`、`TIFF`、`EPS`）、分辨率�?00 DPI 以上为佳）、路�?→ `OK`�?    

---

### 💡 高效技�?
- **�?RBQL 预筛选大文件**  
    如果仪器输出的数据有几百万行，直接在 Origin 中操作会很卡。可先用 RBQL 的 `WHERE` 筛选出需要的区间，保存为新文件再导入�?    
- **Origin 模板批量出图**  
    处理完一张图后，右键图形窗口标题�?→�?*Save Template As...**，存为模板。下次导入新数据后，点击菜单 `Plot` → `Template Library...` 选择该模板，所有格式瞬间自动套用�?    
- **中文显示修复**  
    �?Origin 图中中文显示为方框，双击文字，将字体改为系统自带的�?*宋体** 或�?*黑体** 即可�?    

---

### 📋 快捷键统一速查

|环境|快捷�?指令|功能|
|---|---|---|
|VS Code|`Ctrl+Shift+P`|打开命令面板（执行所�?Rainbow CSV 指令）|
|VS Code|`Ctrl+H`|查找替换（批量改分隔符）|
|VS Code|`Ctrl+Shift+S`|另存为（清洗后保存为 CSV）|
|VS Code|`Ctrl+Shift+P` → `Rainbow CSV: RBQL`|启动�?SQL 查询|
|VS Code|点击状态栏 **CSVLint**|数据一致性检查|
|Origin|拖拽文件到工作区|弹出智能导入向导|
|Origin|左下方工具栏|快速创建折线图/散点图等|
|Origin|双击图形元素|弹出对应的格式设置窗口|
|Origin|右键图形空白�?�?Export Graphs|导出为高质量图片|
##  通用实验数据 CSV 模板 · 使用指南

### 🧱 一、模板结构说�?
你的所有数据文件都�?*三个固定部分**组成�?
text

# 注释行（�?# 开头，记录实验信息，Origin 会自动忽略）
表头�?�?自变量X,因变量Y1(单位),因变量Y2(单位),备注
数据�?�?1,0.1,0.2,
数据�?�?2,0.3,0.5,
数据�?�?3,0.5,0.8,

**必须遵守的规则：**

- **列数绝对一�?*：表头有多少个逗号，每一行数据就必须有同样多的逗号�?    
- **单位写在表头**：用 `物理�?单位)` 格式，如 `温度(�?`，数据单元格里只写数字�?    
- **第一列为 X �?*：把时间、浓度、序号等自变量放在最左边�?    

---

### ✏️ 二、按需自定义模�?
每次实验前，复制一份 `通用实验模板.csv`，然后按实际需求修改�?
|你的实验情况|操作|
|---|---|
|**只有一�?X-Y**（如温度-电压）|删除多余�?Y 列，表头改为 `温度(�?,电压(V)`|
|**有多�?Y**（如时间-浓度A-浓度B）|增加 Y 列，表头改为 `时间(min),浓度A(mg/L),浓度B(mg/L)`|
|**需要记录文�?*（如样品编号）|保留“备注”列，文本随意填写|
|**没有备注**|直接删掉“备注”列|
|**第一列为文本**（如样品名）|可以，但 Origin 会将其当�?X 轴。若要作为分组标签，导入后在 Origin 里右键该列，设为“Categorical”|

---

### 🔍 三、用 VS Code 快速检查格�?
填完数据后，别急着导入，先�?Rainbow CSV 做两秒自检�?
1. **彩虹着色判断列对齐**  
    �?VS Code 打开 CSV，每一列都会染上不同颜色。如果某行颜色出现断裂，说明这一行的逗号（列数）错了�?    
2. **一键全表检�?*  
    点击 VS Code 底部状态栏的�?*`CSVLint`** 按钮，它会瞬间标出所有列数不一致的行�?    
3. **有错就改**  
    配合 `Ctrl+H` 可批量替换分隔符；多余的逗号直接删除即可�?    

---

### 📥 四、导�?Origin（固定三步，永不变化�?
无论数据维度怎么变，这三步操作完全一样�?
1. **拖拽**：把 `.csv` 文件直接拖进 Origin 的灰色工作区�?    
2. **核对设置**�?    
    - `Long Names` = `1`（指定第一行为表头�?        
    - `Separator` = `Comma`（逗号分隔�?        
3. **确认预览**：下方预览显示数据正确分�?�?点击 `OK`�?    

导入后，选中列，点击下方工具栏的图形按钮即可出图�?
---

> 💡 **记住这条铁律**：每次实验只需做两件事 ——�?*1. 复制模板并改名；2. 按列填数�?*。其余所有格式检查交�?VS Code，导入识别交�?Origin�
